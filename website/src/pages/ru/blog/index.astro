---
import { getCollection } from "astro:content";
import BlogIndex from "@/components/BlogIndex.astro";
const entries = (await getCollection("blog", ({ data }) => data.locale === "ru" && !data.draft)).sort((a, b) => +b.data.publishedAt - +a.data.publishedAt);
---
<BlogIndex locale="ru" entries={entries} />
