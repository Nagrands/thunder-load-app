---
tags: [randomizer, вкладки, ui, thunderload]
alias: Вкладка Randomizer
---

# 🎲 Вкладка Randomizer

Описание реализации рандомайзера: как собирается UI, какой поток работы, какие модули задействованы и что улучшать дальше.

---

## 🧩 Структура и сборка UI

- Контент создается целиком в `src/js/modules/views/randomizerView.js` (генерация DOM, события). Обертка монтируется таб-системой из `renderer.js` в `#randomizer-view-wrapper`.  
- Стили и анимации лежат в `src/scss/tab/_randomizer.scss` (герой, сетка карточек, чипы, тумблер без повторов, карточка результата/истории).  
- Есть настройка в Settings для отключения вкладки (`randomizerDisabled`), логика в `settings.js` блокирует контролы и шлет событие `randomizer:toggleDisabled`.

---

## 🔄 Пользовательский поток

1) **Добавление вариантов** — поле ввода, кнопка "Добавить", вставка списка из буфера (разделители: новая строка, `;`, `,`), кнопка "Пример" для наполнения дефолтным списком, очистка всех вариантов.  
2) **Без повторов** — тумблер `#randomizer-no-repeat` хранит пул в `localStorage` (`randomizerPool`), при выключении работает с исходным списком.  
3) **Перемешивание** — кнопки "Запустить"/"Ещё раз" стартуют roll, при no-repeat вытаскивается элемент из пула, иначе — случайный по всему списку. Результат показывает время выбора.  
4) **История** — хранится до 15 записей (`randomizerHistory`), клик по записи подставляет результат и помечает "Выбрано ранее".  
5) **Экспорт/копирование** — кнопка "Скопировать список" (в буфер, `\n`-разделитель) и "Скопировать результат"; кнопка "Сброс пула" обновляет non-repeat набор.  
6) **Персистентность** — варианты, история, настройки и пул лежат в `localStorage` под ключами `randomizerItems`, `randomizerHistory`, `randomizerSettings`, `randomizerPool`.

---

## 🗺️ Модули и связи

- **UI/логика**: `randomizerView.js` — генерация разметки, события, хранение/чтение `localStorage`, тосты, тултипы.  
- **Глобальные зависимости**: `renderer.js` (создает wrapper и подключает TabSystem), `tabSystem.js` (хранит активную вкладку, читает `randomizerDisabled`), `hotkeys.js` (быстрые клавиши на активацию вкладки).  
- **Настройки**: `settings.js` — переключатель отключения вкладки, рассылка события `randomizer:toggleDisabled` и блокировка контролов.  
- **Стили**: `src/scss/tab/_randomizer.scss` + базовые компоненты (кнопки, тултипы) из общих SCSS.

---

## ⚙️ Ключевые особенности

- Поддержка режима "без повторов" с отдельным пулом, переживающим перезапуск.  
- Быстрое добавление списков через буфер и кнопка примера.  
- Короткая история результатов с возможностью повторного выбора кликом.  
- Копирование списка/результата, сброс пула, тосты/тултипы для обратной связи.  
- Вкладку можно временно выключить из настроек (полная блокировка контролов).

---

## 🧭 Дорожная карта улучшений

**Ближайшие**  
- [P1] Добавить веса/приоритеты у вариантов (слайдер/ввод числа) и учитывать их в выборе.  
- [P1] Улучшить UX списка: инлайн-редактирование чипов, drag-and-drop сортировка, массовое удаление выбранных.  
- [P2] Расширить вставку/экспорт: поддержка CSV/TSV, автоочистка пустых строк, предпросмотр вставляемых элементов с подсветкой дублей.  
- [P2] Показать мини-статистику: сколько раз выпадал каждый вариант, сколько осталось в пуле (для no-repeat).

**Среднесрочные**  
- [P2] Шеринг: кнопка "Поделиться" (копия JSON/текстового списка) и импорт из файла.  
- [P2] Мультипулы/пресеты: сохранить несколько списков, быстро переключать, выбирать дефолтный при старте.  
- [P3] Таймер/авто-roll: автозапуск по интервалу с остановкой по условию, звуковое/визуальное уведомление результата.  
- [P3] Расширенные уведомления: system notification при свернутом окне, история с поиском/фильтром по строке.

**Долгосрочные**  
- [P3] Синхронизация между устройствами (хранилище в облаке/файле, импорт/экспорт ключа).  
- [P3] Интеграция с другими вкладками: отправка выбранного результата в Downloader/Backup в один клик (например, как URL или имя профиля).  
- [P3] Тесты: юнит-тесты логики пула/истории, e2e сценарии добавления/ролла/экспорта.

