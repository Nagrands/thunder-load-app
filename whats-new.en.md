<!-- version: 1.5.0 -->

# Release date: June 1, 2026

---

| What changed                                                                             | What you get                                                                                                                                                                                                                                                          |
| ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Stale preview requests are now cancelled                                                 | When you quickly replace a Downloader URL, the app asks the main process to stop the old preview request instead of keeping `yt-dlp` busy with an outdated link                                                                                                      |
| The Download button now prepares quality data ahead of the click                         | Hovering or focusing the button safely warms the full-info cache, so the following click is more likely to open quality selection or compact downloading faster                                                                                                      |
| Reopening preview hits the source less often                                             | Re-pasting the same link or using Retry download now reuses fresh preview cache instead of starting another request when the data is already available                                                                                                                  |
| Downloader responds faster to pasted and dropped links                                   | A ready URL from paste or drag and drop now starts preview fetching immediately, without the debounce delay that still protects manual typing                                                                                                                          |
| Downloader now warms quality data after a successful preview                              | Once a link is recognized, the app prepares full format data in the background, so compact mode and the quality dialog are more likely to open quickly without delaying the first preview                                                                             |
| Downloader now merges duplicate video data requests                                      | When preview, queue titles, compact mode, and the quality dialog ask for the same link at the same time, the app reuses one shared request and cache instead of repeating the fetch                                                                                  |
| Downloader shows previews faster after pasting a link                                    | Initial preview now uses a separate lightweight request without loading the format list; full data is loaded only when quality selection or downloading needs it                                                                                                      |
| Video info fetching through `yt-dlp` is faster                                           | The app repeats binary checks less often, reuses normal video metadata longer, and handles YouTube links to a specific video more precisely                                                                                                                            |
| Compact Downloader no longer opens the quality dialog after pasting a link                | Compact mode now relies on its inline quality selectors and does not auto-open the modal, including Retry download from history                                                                                                                                        |
| Appearance settings now use a compact panel                                               | Language, text size, theme, and low-effects mode are grouped into one cleaner panel that is easier to scan and adjust                                                                                                                                                 |
| Tools navigation is now more compact                                                     | The back button and breadcrumbs now share one row, and the trail starts directly with Tools without the extra Thunder label                                                                                                                                            |
| Fixed deleting failed history entries from the context menu                               | Entries with the Error status can now be removed by right-clicking them even when there is no file or known size on disk                                                                                                                                              |
| Downloader now has a compact quality mode                                                | Switch above the URL field to a compact view with preview, video and audio selectors, and Download starts the selected combination without opening an extra dialog                                                                                                     |
| The quality picker Audio section now includes MP3                                        | You can choose an MP3 track directly in the quality dialog; the app downloads the best available audio stream and converts it through ffmpeg                                                                                                                           |
| The download queue can now be reordered by dragging                                      | Grab a row by the dots on the left and drag it up or down to change the order of pending downloads without using separate move buttons                                                                                                                                |
| Quality selection now opens automatically after a recognized paste                       | When a pasted link resolves with a preview and formats, the quality picker opens by itself; the behavior can be disabled in Downloader settings                                                                                                                       |
| Queue start now lets you choose the launch mode                                          | When the queue has multiple items, Start asks whether to download only the first video or launch the whole queue; choosing the whole queue keeps the configured parallel download behavior                                                                            |
| The download quality dialog has a new compact design                                     | Format choices are easier to scan, the selected option is clearer, and source/preview actions now live in clean icon buttons while the existing download and queue flows stay the same                                                                                |
| Added background and live preview for YouTube links in Downloader                        | Pasting a YouTube URL still enables the muted decorative background preview, and now an explicit button can open a separate modal player with sound; when the preview source is refreshed, the player tries to continue the same video from roughly the same position |
| Downloader tools status moved into the footer top-state                                  | The `yt-dlp`, `ffmpeg`, and `Deno` block now lives next to the current section label in the lower bar, appears only near the top of the page, and still respects the existing Settings toggle                                                                         |
| Section navigation now migrates from the header into the footer                          | Tabs stay in the header near the top of the page, then move into the lower bar while scrolling down together with a quick back-to-top action                                                                                                                          |
| Hash Check now has a compact redesign and fixed Drag and Drop on Windows                 | File selection and verification settings take less space, there is a quick reset action, and dropped Windows paths plus `file://` URIs are parsed correctly again                                                                                                     |
| File Sorter now uses a simpler layout, and the Russian locale labels it as «Сортировщик» | Preview, sorting parameters, workspace controls, and tool settings are grouped into a more compact flow, so the working path is easier to scan and operate                                                                                                            |
